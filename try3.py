
import numpy as np
import torch
DEG2RAD = np.pi / 180.
RAD2DEG = 180. / np.pi
a = np.array([1, 2, 3, 4, 5, 6, 7, 8])
print([12 // 2 * i + j
                            for i in range(2) for j in range(2)])
print(np.linalg.norm(np.array([0.7, 0, 0.8]) - np.array([0.2, 0, 1]), axis=-1))